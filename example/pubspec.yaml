name: example
description: A new Flutter project.
publish_to: 'none'
version: 0.1.0

environment:
  sdk: '>=3.1.0 <4.0.0'

dependencies:
  flame: 
    path: ../../flame/packages/flame
  flame_liquid:
    path: ../
  flutter:
    sdk: flutter
  liquid:
    path: ../../liquid

dev_dependencies:
  flutter_lints: ^2.0.0
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true
